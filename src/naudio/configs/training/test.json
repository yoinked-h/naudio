{
    "model": "stable-audio-open",
    "training_hparams": {
        "lr": 0.0005,
        "lr_scheduler": {
            "type": "cosine",
            "decay_steps": 1000
        },
        "optimizer": {
            "type": "adamw",
            "betas": [0.9, 0.999],
            "weight_decay": 0.05
        }
    },
    "batch_size": 1,
    "dataset": "test",
    "num_epochs": 1,
    "save_every": 10000,
    "seed": "phantasmagoira"
}